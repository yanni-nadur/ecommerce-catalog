import { Container } from "react-bootstrap";

const Homepage = () => {
	return (
		<Container className="text-center mt-5">
			<h1>Bem-vindo ao E-Commerce</h1>
			<p>Faça login ou registre-se para acessar o painel administrativo.</p>
		</Container>
	);
};

export default Homepage;
